# ============================================================================
# CORE BASE CONFIGURATION
# ============================================================================
# Minimal foundation that works for any developer on any platform.

environment:
  proxy:
    enabled: false

git:
  user:
    name: "${USER}"
    email: "${USER}@example.com"

tools_required:
  - git

tools_available:
  - docker
  - python3
  - nodejs
