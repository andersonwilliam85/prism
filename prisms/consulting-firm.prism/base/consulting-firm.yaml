# ============================================================================
# CONSULTING FIRM BASE CONFIGURATION
# ============================================================================

tool_registry_file: "tool-registry.yaml"

firm:
  name: "[Consulting Firm Name]"
  domain: "consultingfirm.com"

environment:
  proxy:
    enabled: false
  vpn:
    required: false

git:
  user:
    name: "${USER}"
    email: "${USER}@consultingfirm.com"

tools_required:
  - git
  - docker
  - python3

tools_optional:
  - vscode
  - postman
  - terraform
